# HW6 API versions plus the CPU physics versions already used on this Mac.
# Kept separate from the course's Linux uv.lock.
mjlab==1.2.0
rsl-rl-lib==5.0.1
torch==2.9.0
mujoco==3.8.1
mujoco-warp==3.9.0.1
warp-lang==1.14.0
pytest>=9.0.2
ruff>=0.14.14
# mjlab imports scipy for terrains, but 1.2.0 omits it from its metadata.
scipy==1.16.2
